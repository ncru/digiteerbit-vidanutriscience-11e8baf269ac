$('#orders-list').DataTable().draw();
$('#order_status_modal').modal('hide');
$('.notice-container').html("<%= j (render 'notice') %>");

<% if error.present? || notice.present? %>
  $('html, body').animate({ scrollTop: 0 }, 'slow', 'swing');
<% end %>
